<section>
	<% @posts.each do |p| %>
		<div class='colsBLOG'>
			<div class='colBLOG col1BLOG'>
				<h3><%= link_to p.title, post_path(p) %></h3>
				<p class='ref'>created <%= time_ago_in_words(p.created_at) %> ago</p>
				<p class='tag'>Tags: <%= raw p.tags.map(&:name).map { |t| link_to t, tag_path(t) }.join(', ') %>
				</p>
				<div class='buttons'>	
				<%= link_to 'Edit', edit_post_path(p), class: 'is-small button' %>
				<%= link_to 'Delete', post_path(p), method: :delete, class: 'is-small button' %>
				</div>
			</div>
			<div class="colBLOG col2BLOG"><p class=''><%= p.body %></p></div>	
		</div>
	<% end %>
</section>